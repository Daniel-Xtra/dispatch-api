import { Module } from '@nestjs/common';

/**
 * Channel provider ports and selection logic (Phases 8–9).
 * Vendor SDKs live under infrastructure/provider-adapters — not here.
 */
@Module({})
export class ProvidersModule {}
